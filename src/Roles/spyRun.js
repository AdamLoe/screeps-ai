let setTask = require("../Creeps/setTask");

module.exports = (creep) => {
	//Here is where we decide where we want to spy
		//Will only call this whenever we just finished spying something or something came up that stopped us
};